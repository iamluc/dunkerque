{{ form_start(form, {'attr': {'class': 'navbar-form navbar-right', 'role': 'search'}}) }}
    <div class="input-group">
        {{ form_widget(form.keyword, {'attr': {'class': 'form-control', 'placeholder': ('search.find'|trans), 'autofocus': 'autofocus'}}) }}
        <span class="input-group-btn">
            <button type="submit" class="btn btn-default" id="search_submit"><span class="glyphicon glyphicon-search"></span></button>
        </span>
    </div>
{{ form_end(form) }}
