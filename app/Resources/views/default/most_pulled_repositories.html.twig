<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">{{ 'repositories.most_pulled'|trans }}</h3>
    </div>
    <div class="panel-body">
        <ul>
            {% for repo in repositories %}
                <li>
                    <a href="{{ path('repository', {name: repo.name}) }}">{{ repo.name }}</a>
                    <span class="pull-right">{{ repo.pulls }} <span class="glyphicon glyphicon-download"></span></span>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
